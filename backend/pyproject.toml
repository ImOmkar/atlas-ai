[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Omkar Parab", email = "omkar211196@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.140.13",
    "pydantic-settings>=2.14.2",
    "uvicorn>=0.51.0",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
